pre-commit:
  parallel: true
  commands:
    prettier:
      # run: pnpm run prettier:check
      description: "Check for prettier formatting"
      run: npx prettier --check {staged_files} --ignore-path=.prettierignore
      glob: "*.{js,ts,md,json,yml,yaml}"
      exclude: "node_modules/**/*"
