pre-commit:
  parallel: true
  commands:
    prettier:
      description: "Check for prettier formatting"
      run: pnpm run prettier:check **/*.{json,md,js,ts,yml,yaml}
      exclude: "node_modules/**/*"
